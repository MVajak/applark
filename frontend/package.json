{
  "name": "postpilot-frontend",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.19.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @postpilot/web dev",
    "build": "pnpm --filter @postpilot/web build",
    "preview": "pnpm --filter @postpilot/web preview",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "gen:api": "pnpm --filter @postpilot/web gen:api",
    "typecheck": "pnpm --recursive typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "typescript": "~6.0.2"
  }
}
